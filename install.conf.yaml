- defaults:
    link:
      relink: true
      create: true

- clean: ['~', '~/.config', '~/.keymaps']

- link:
    ~/.dotfiles: ''
    ~/.config/bspwm:
    ~/.config/sxhkd:
    #/etc/udev/hwdb.d/90-zboard-merc.hwdb: 'hwdb.d/90-zboard-merc-normal-no-caps-lock.hwdb'
    ~/.keymaps:
    ~/.bashrc:
    ~/.xprofile:
    ~/.gitconfig:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
