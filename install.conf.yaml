- defaults:
    link:
      relink: true
      create: true

- clean: ['~', '~/.config']

- link:
    ~/.dotfiles: ''
    ~/.config/bspwm:
    ~/.config/sxhkd:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
