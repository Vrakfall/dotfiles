- defaults:
    link:
      relink: true
      create: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.config/bspwm:
    ~/.config/sxhkd:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
